name: Auto-merge minor/patch updates
on:
  schedule:
    - cron: "* * * * *"
jobs:
  test:
    name: Auto-merge minor and patch dependency updates
    runs-on: ubuntu-latest
    steps:
      - uses: koj-co/dependabot-pr-action@master
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          merge-minor: true
          merge-patch: true
